# fits-view ユースケース一覧

> **対象読者**：プロダクトオーナー・実装担当者
> **目的**：DS9等の代替として「FITS IDE」が対応すべき業務シナリオを網羅し、実装優先度の根拠とする
> 最終更新：2026-05-31

---

## ユーザーペルソナ

| ID | ペルソナ | 代表的な業務 |
|---|---|---|
| **P1** | 観測天文学者 | 望遠鏡観測後のデータ確認・測光・論文用図版作成 |
| **P2** | データパイプラインエンジニア | 自動処理パイプラインの開発・CI/CD・品質監視 |
| **P3** | 大学院生・研究者 | 日常的なデータ閲覧・解析・Python連携 |
| **P4** | 分光観測者 | IFU・スペクトルキューブの解析 |
| **P5** | X線・電波天文学者 | イベントリスト・非標準FITS形式の扱い |

---

## UC-01 クイックルック（観測終了直後の目視確認）

**アクター**：P1, P3
**トリガー**：観測夜に望遠鏡から転送されたFITSファイルを受け取る
**ゴール**：数秒以内に画像を表示し、観測が成功したか判断する

### シナリオ

1. ターミナルで `fits-view .` を実行してディレクトリを開く
2. ファイルエクスプローラーから目的のFITSをクリック
3. ZScaleで即座に表示される
4. ヘッダパネルで `EXPTIME`, `FILTER`, `AIRMASS`, `RA`, `DEC` を確認
5. ステータスバーでカーソル下のピクセル値・WCS座標を確認
6. ズームイン/アウトで星像・銀河の形状を目視確認

### 受け入れ基準

- 1GB以内のFITSは1秒以内に表示される
- ヘッダ検索でキーワードをすぐに見つけられる
- マウスオーバーで座標とピクセル値がステータスバーに表示される

### 対応機能

ファイルエクスプローラー / ZScaleデフォルト / ヘッダパネル / WCS座標表示

---

## UC-02 画像品質評価（PSF・バックグラウンド確認）

**アクター**：P1, P3
**トリガー**：クイックルック後に観測品質を定量的に確認したい
**ゴール**：PSFのFWHM・背景ノイズ・飽和ピクセルの有無を把握する

### シナリオ

1. ヒストグラムパネルを開いて pixel value 分布を確認
2. 明るい星にズームしてクロスヘアを置き、中心ピクセル値を読む
3. Line Profile ツールで星を横断するプロファイルを取りガウシアンFWHMを測定
4. 統計パネルで `mean`, `std`, `min`, `max` を確認
5. アパーチャーフォトメトリーで SNR を概算
6. 飽和星（ピクセル値 ≥ 上限）の分布をヒストグラムで確認

### 受け入れ基準

- ヒストグラムの vmin/vmax ドラッグで画像が即座に再描画される
- Gaussian fit が FWHMを arcsec 単位で返す
- 統計パネルに median / std / 飽和ピクセル数が表示される

### 対応機能

ヒストグラムパネル / 1Dプロファイル抽出 / 統計パネル / アパーチャーフォトメトリー

---

## UC-03 複数ファイルの比較（ブリンク・分割ビュー）

**アクター**：P1, P3
**トリガー**：複数フィルター・複数エポックの画像を比較したい
**ゴール**：ターゲット天体の変化・フィルター間の形態差を目視確認する

### シナリオ

1. 複数のFITSをタブで開く
2. `Ctrl+\` で SideBySide 分割ビューに切り替える
3. 「連動パン・ズーム」をオンにして両ペインを同期させる
4. `Ctrl+L` でブリンクモードを起動して変光天体を探す
5. WCS 基準での同期（ピクセル座標のずれを吸収）

### 受け入れ基準

- 連動モードで片方をパンすると他方が WCS 基準で追随する
- ブリンクは 1〜10fps で切り替え速度が設定できる
- WCS なしファイルはピクセル座標で連動する

### 対応機能

マルチタブ / 分割ビュー / 連動パン・ズーム / ブリンクモード

---

## UC-04 カラーマップ・スケール調整（可視化チューニング）

**アクター**：P1, P3, P4
**トリガー**：デフォルト表示では淡い構造が見えない
**ゴール**：科学的に意味のある表示設定を素早く見つける

### シナリオ

1. ヒストグラムパネルで vmin/vmax を手動でドラッグ調整
2. カラーマップを Gray → Viridis → Plasma と切り替えて確認
3. スケールを ZScale → Log → ASinh と試す
4. 右ドラッグでコントラスト/バイアスをファインチューニング
5. 設定をセッションに保存して再起動後も再現

### 受け入れ基準

- vmin/vmax のドラッグから画像再描画まで 100ms 以内
- カラーマップ切り替えはドロップダウン1クリックで完了
- セッション保存後の再起動でスケール設定が完全に復元される

### 対応機能

ヒストグラムパネル / カラーマップ / スケーリングアルゴリズム / セッション管理

---

## UC-05 WCS座標確認・グリッドオーバーレイ

**アクター**：P1, P3
**トリガー**：「この天体の正確な座標は？」「FOV のどのあたりにターゲットがいる？」
**ゴール**：RA/Dec をすぐに読み、空間的な位置感覚を掴む

### シナリオ

1. ステータスバーで常時 RA/Dec を確認しながらマウスを動かす
2. `Ctrl+G` で WCS グリッドオーバーレイを表示
3. ズームに応じて格子間隔が自動調整される（10°→1°→10'→1'→10"）
4. グリッドの色・透明度を設定で調整

### 受け入れ基準

- TAN投影ファイルでグリッドが正しい位置に描画される
- ズームイン時に格子間隔が自動細分化される
- WCS なしファイルではグリッドボタンがグレーアウト

### 対応機能

WCS座標表示 / WCSグリッドオーバーレイ / 投影法対応（TAN/SIN/ZEA/AIT）

---

## UC-06 アパーチャー測光

**アクター**：P1, P3
**トリガー**：特定の星・銀河のフラックス・等級を手動で測りたい
**ゴール**：SNR・フラックス・等級（ゼロ点補正）を得てCSVに保存する

### シナリオ

1. コマンドパレット → "Photometry Mode"
2. 画像上でターゲット星をクリックし、アパーチャー（内円・外環）を配置
3. 測定結果パネルで `flux`, `SNR`, `magnitude` を確認
4. 複数ターゲットに追加配置して一括測定
5. `MAGZPT` / `PHOTZP` キーワードが存在すれば自動で等級換算
6. CSV / VOTable でエクスポート

### 受け入れ基準

- クリック1回でアパーチャーが配置され結果が即表示される
- バックグラウンドアニュラスの内径・外径を GUI で変更できる
- 等級ゼロ点をヘッダから自動読み込みする

### 対応機能

アパーチャーフォトメトリー / WCS座標対応 / CSV/VOTableエクスポート

---

## UC-07 マルチバンドRGB合成

**アクター**：P1, P3
**トリガー**：r/g/b バンドの画像を疑似カラー合成してプレゼン・論文用に使いたい
**ゴール**：R・G・Bにバンドを割り当て、スケール調整して合成画像を得る

### シナリオ

1. 3枚の FITS をタブで開く
2. コマンドパレット → "RGB Composite..."
3. ダイアログで R/G/B にタブを割り当て
4. チャンネルごとに `scale=ZScale`, `vmin`, `vmax` を設定
5. 結果が新タブとして表示される
6. `fits-view render r.fits g.fits b.fits --rgb --output composite.png` でCLIからも同様に実行

### 受け入れ基準

- WCS 整合が取れない場合に警告が出る
- チャンネルごとのスケール設定が独立して機能する
- CLI の `--rgb` オプションで PNG に書き出せる

### 対応機能

RGBコンポジット / CLI render拡張

---

## UC-08 コントゥアーオーバーレイ（電波+光学の重ね合わせ）

**アクター**：P1, P5
**トリガー**：電波強度マップを光学画像に重ねてジェット・電波銀河を確認したい
**ゴール**：2 つの画像の等高線を WCS 座標で重ね合わせる

### シナリオ

1. 光学 FITS と電波 FITS をタブで開く
2. 光学タブを選択し、コマンドパレット → "Add Contour Overlay..."
3. ソースに電波タブを選択
4. コントゥアーレベルを `3σ, 5σ, 10σ` で自動計算
5. WCS 座標変換して正確な位置に重ねる

### 受け入れ基準

- WCS が一致すれば自動座標変換でオーバーレイされる
- マーチングスクエアズで等高線が滑らかに描かれる
- 等高線の色・線幅・透明度を調整できる

### 対応機能

コントゥアーオーバーレイ / WCS座標変換

---

## UC-09 カタログオーバーレイ（天体同定）

**アクター**：P1, P3
**トリガー**：「この点源は何の天体か？」「視野内の既知天体を全部表示したい」
**ゴール**：SIMBAD / Gaia / VizieR からオブジェクトを取得して画像上に表示する

### シナリオ

1. WCS付きFITSを開く
2. コマンドパレット → "Query Catalog..."
3. カタログを `SIMBAD` / `Gaia DR3` / `VizieR` から選択
4. 検索半径・最大件数を設定して "Query"
5. FOVを自動認識してオブジェクトをシンボルで描画
6. シンボルをクリックして天体名・等級・タイプのポップアップを表示
7. ローカルのVOTableファイルも `Ctrl+R` から読み込める

### 受け入れ基準

- SIMBAD 10arcmin クエリが 3秒以内に完了する
- オフライン時はキャッシュを使い graceful degradation
- 等級・天体タイプでのフィルタリングができる

### 対応機能

カタログオーバーレイ / VOTableサポート / ネットワーク非同期クエリ

---

## UC-10 1Dプロファイル抽出（銀河輝度プロファイル・PSF）

**アクター**：P1, P3
**トリガー**：銀河の表面輝度プロファイルを測りたい / PSF の FWHM を確認したい
**ゴール**：ドラッグで引いた直線に沿った輝度値を取得し、Gaussian fit する

### シナリオ

1. コマンドパレット → "Line Profile Tool"
2. 画像上をドラッグして直線を定義
3. プロットパネルに輝度プロファイルが表示される
4. WCS 有効時は横軸が arcsec / arcmin で表示
5. "Gaussian Fit" ボタンで FWHM を自動計算
6. "Export CSV" でデータを保存

### 受け入れ基準

- ドラッグ終了後即座にプロットが表示される
- Gaussian fit の結果に FWHM が arcsec 単位で表示される
- 複数プロファイルを同一グラフに重ねられる

### 対応機能

1Dプロファイル抽出 / プロットパネル / Gaussian fit / CSVエクスポート

---

## UC-11 データキューブ閲覧（分光・IFU・時系列）

**アクター**：P4, P1
**トリガー**：VLT/MUSE や JWST/NIRSpec の IFU データを解析したい
**ゴール**：波長スライスを操作しながらスペクトル構造を把握する

### シナリオ

1. `NAXIS=3` の FITS を開くと自動でキューブモードが起動
2. キューブパネルのスライダーで z 軸（波長）を移動
3. 物理値（波長 nm / 周波数 GHz）が軸ラベルに表示される
4. "Play" でアニメーション再生（FPS 設定可能）
5. 画像上の点をクリックして `(x,y)` を固定 → スペクトルをプロットパネルに表示
6. 波長範囲を指定してコラプス（Sum / Mean）した積分マップを表示

### 受け入れ基準

- GB 級のキューブを RAM に全ロードせずスライス単位でストリーミング読み込み
- スライダー操作からフレーム更新まで 200ms 以内
- スペクトル抽出後、プロットが CSV エクスポートできる

### 対応機能

データキューブ / スペクトル抽出 / アニメーション再生 / コラプスモード

---

## UC-12 X線イベントリスト → 画像変換

**アクター**：P5
**トリガー**：Chandra / XMM-Newton の BINTABLE イベントリストを画像として見たい
**ゴール**：X・Y列をビニングしてカウントマップを生成・表示する

### シナリオ

1. BINTABLE 拡張を含む FITS を開く
2. イベントリスト検出時に自動でカウントマップ変換を提案
3. ビニングサイズを選択（例：1px / 2px / 4px）
4. 生成されたカウントマップを通常の画像として表示
5. エネルギーバンドで絞り込み（`ENERGY` 列）

### 受け入れ基準

- `X`, `Y`（または `DETX`, `DETY`）列を自動検出してビニング
- ビニング後に通常の ZScale 表示・アパーチャー測光が使える
- エネルギーバンドフィルターをスライダーで設定できる

### 対応機能

BINTABLEイベントリスト変換 / エネルギーフィルター / カウントマップ

---

## UC-13 DS9リージョンファイルの利用

**アクター**：P1, P3, P5
**トリガー**：以前の解析で作ったリージョン定義（`.reg`）を新しい画像に重ねたい
**ゴール**：DS9 形式 `.reg` ファイルを読み込んで円・矩形・多角形を画像上に表示する

### シナリオ

1. `Ctrl+R` でリージョンファイルを選択
2. 円・矩形・多角形のシェイプがオーバーレイ表示される
3. WCS 座標定義のリージョンは自動で変換して表示
4. シェイプをクリックして属性（色・ラベル・座標）を確認
5. アパーチャー測光のリージョンとして再利用

### 受け入れ基準

- DS9 の `image`, `physical`, `fk5` 座標系を正しく解釈する
- 複数リージョンファイルを重ねて表示できる
- セッションに保存・復元できる

### 対応機能

DS9リージョンファイル表示 / セッション管理

---

## UC-14 画像演算（差分・フラット補正確認）

**アクター**：P1, P2
**トリガー**：キャリブレーション前後の差分を確認したい / S/N マップを作りたい
**ゴール**：2枚の画像を算術演算して結果を新タブに表示する

### シナリオ

1. 補正前・補正後の 2枚を開く
2. コマンドパレット → "Image Arithmetic..."
3. A = 補正後、B = 補正前、演算 = `(A-B)/B`（相対差）を選択
4. 結果が新タブに表示される
5. `fits-view arithmetic a.fits - b.fits --output diff.fits` でCLI からも実行

### 受け入れ基準

- 演算 `+, -, *, /, (A-B)/B, A/B` をサポート
- 画像サイズ不一致時に警告が出てキャンセルできる
- 結果をFITSとして保存できる

### 対応機能

画像演算 / CLIサブコマンド拡張

---

## UC-15 バッドピクセルマスクの確認

**アクター**：P2, P1
**トリガー**：パイプラインが生成したマスクが正しく機能しているか確認したい
**ゴール**：マスクFITSを半透明で科学画像に重ね、どのピクセルがマスクされたか把握する

### シナリオ

1. 科学画像を開く
2. コマンドパレット → "Load Mask..."
3. マスクFITSを選択（非ゼロ値 = マスク済み）
4. 半透明の赤でオーバーレイ表示
5. ビット値ごとに異なる色で分類（コスミックレイ=赤、飽和=黄、etc）

### 受け入れ基準

- ビットフラグ（複数ビットマスク）に対応
- 色・透明度を GUI から変更できる
- マスクされたピクセルの統計（比率）が表示される

### 対応機能

マスクオーバーレイ / ビットフラグ対応

---

## UC-16 セッション管理（作業の中断・再開）

**アクター**：P1, P3
**トリガー**：翌日の解析作業を前日と同じ状態から再開したい
**ゴール**：開いたファイル・表示設定・ズーム・アノテーションをすべて復元する

### シナリオ

1. コマンドパレット → "Save Session..." で `.fvs` ファイルを保存
2. アプリを閉じる（最後のセッションは自動保存される）
3. 翌日 `fits-view --session obs_20260531.fvs` で再起動
4. タブ・カラーマップ・zoom・アノテーション・カタログオーバーレイが全て復元
5. 同僚に `.fvs` ファイルを共有して同じビューを再現

### 受け入れ基準

- 相対パスで保存されポータブルなセッションファイル
- セッション形式は人間が読める JSON
- `fits-view --session` で CLI から指定して起動できる

### 対応機能

セッション管理 / アノテーション / `.fvs` 形式

---

## UC-17 アノテーション追加（画像への注釈）

**アクター**：P1, P3
**トリガー**：「この点源が研究対象」「この領域が飽和してる」を画像に直接メモしたい
**ゴール**：円・矩形・直線・テキストを WCS 座標で画像に貼り付ける

### シナリオ

1. コマンドパレット → "Annotation Mode"
2. 画像上をドラッグして円を描く → ラベルを入力
3. テキストアノテーションを配置
4. WCS 付きファイルでは RA/Dec で保存（別エポックの画像でも同じ場所に表示）
5. セッションに保存

### 受け入れ基準

- アノテーション追加・編集・削除が直感的に操作できる
- WCS 座標で保存したアノテーションが別の WCS 画像でも正確な位置に描画される
- DS9 の `.reg` 形式でエクスポートできる

### 対応機能

アノテーション / WCS座標 / セッション管理

---

## UC-18 論文用図版の書き出し

**アクター**：P1, P3
**トリガー**：査読論文・プレゼン用の高品質な画像をプログラマブルに生成したい
**ゴール**：カラーバー・軸ラベル・スケールバー付きの PDF/PNG を生成する

### シナリオ

1. GUI で表示設定を整える（カラーマップ・vmin・vmax）
2. コマンドパレット → "Export Publication Figure..."
3. フォーマット（PNG 300dpi / PDF / SVG）・カラーバー有無・タイトルを設定
4. または CLI: `fits-view render image.fits --format pdf --dpi 300 --colorbar --wcs-grid --title "NGC 1234 Hα"`

### 受け入れ基準

- 300dpi PNG で論文投稿品質の出力
- PDF / SVG 出力にカラーバーと WCS グリッドが含まれる
- フォントサイズ・余白をオプションで指定できる

### 対応機能

論文品質書き出し / CLI render拡張 / WCSグリッド

---

## UC-19 パイプラインCI/CDでのFITS検証

**アクター**：P2
**トリガー**：パイプラインの各ステージで出力 FITS が仕様通りか自動テストしたい
**ゴール**：FITS の形式・ヘッダ値・統計をスクリプトで検証して合否を返す

### シナリオ

1. `fits-view check image.fits --assert "NAXIS == 2" --assert "BITPIX == -32" --has-wcs`
2. 失敗時に非ゼロ終了コードと失敗箇所のメッセージを返す
3. `fits-view stats image.fits --format json` をパイプで jq に渡す
4. `fits-view render *.fits --output-dir thumbs/ --jobs 8` でバッチサムネイル生成
5. GitHub Actions の YAML に組み込み

### 受け入れ基準

- `--assert` 式で `==, !=, <, >, <=, >=` が使える
- CI 失敗時のエラーメッセージが行番号・キーワード名付きで分かりやすい
- `--output-dir` バッチ処理が CPU 数に応じて並列化される

### 対応機能

CLIサブコマンド（check/stats/render） / バッチ処理 / JSON出力

---

## UC-20 大容量FITSの閲覧（SKA・Subaru HSC・LSST規模）

**アクター**：P2, P1
**トリガー**：10GB〜1TBの FITS ファイルをパン・ズームしながら確認したい
**ゴール**：ファイル全体を RAM に載せずにスムーズなビューを実現する

### シナリオ

1. `fits-view image_100gb.fits` で起動（メモリマップ自動切替）
2. 最初のタイルが 2秒以内に表示される
3. パン・ズームで新しいタイルが非同期でロードされる
4. ズームレベルに応じた LOD（解像度）が自動選択される
5. メモリ使用量が 2GB 以内に制限される

### 受け入れ基準

- 50GB FITS 閲覧中のメモリ使用量が 2GB 以内
- パン・ズームが GPU で 60fps, CPU で 30fps 以上
- タイルロード進捗インジケーターが表示される

### 対応機能

memmap2 タイルストリーミング / LRUキャッシュ / GPU/CPUレンダラー / LOD

---

## UC-21 SAMP連携（TOPCAT・Aladin との相互操作）

**アクター**：P1, P3
**トリガー**：TOPCAT で選択したカタログ行を画像上で確認したい
**ゴール**：SAMP プロトコルで fits-view と TOPCAT / Aladin をリアルタイム連動させる

### シナリオ

1. SAMP ハブを起動（TOPCAT 起動時に自動）
2. コマンドパレット → "SAMP → Connect"
3. TOPCAT でカタログ行を選択 → fits-view が対応座標にカーソルをジャンプ
4. fits-view でカーソルを動かすと TOPCAT に座標をブロードキャスト
5. 測光結果テーブルを SAMP で TOPCAT に送信

### 受け入れ基準

- `image.load.fits`, `coord.pointAt.sky`, `table.load.votable` を受信できる
- 座標ブロードキャストが 500ms 以内に TOPCAT に反映される

### 対応機能

SAMP連携 / VOTable送受信

---

## UC-22 スクリプトコンソールでのカスタム解析

**アクター**：P3, P1
**トリガー**：GUI に存在しない処理を即興でピクセルデータに適用したい
**ゴール**：コンソールに Lua/Python コードを書いてデータを加工し、新タブに表示する

### シナリオ

1. `Ctrl+Shift+C` でスクリプトコンソールを開く
2. `data = fv.active_tab():data()` でピクセルデータを取得
3. カスタム処理（例：log10変換、平滑化）を実装して `fv.show(result)` で表示
4. `fv.active_tab():header()["EXPTIME"]` でヘッダ値を参照
5. コンソール履歴を Jupyter Notebook にエクスポート

### 受け入れ基準

- スクリプトエラーが分かりやすいスタックトレースで表示される
- `fv.show()` で結果が新タブとして即座に表示される
- サンプルスニペット集が付属している

### 対応機能

スクリプトコンソール（Lua → Python順） / PyO3バインディング

---

## UC-23 Pythonライブラリとしての利用（astropy連携）

**アクター**：P3, P2
**トリガー**：Jupyter Notebook の解析パイプラインから fits-view の高速表示を呼びたい
**ゴール**：`import fitsview` で開き、numpy 配列を渡してインタラクティブに確認する

### シナリオ

1. `pip install fitsview` でインストール
2. `img = fitsview.open("image.fits")` → `data = img.data(hdu=1)`（numpy配列）
3. `viewer = fitsview.show(img, colormap="viridis")` でGUI起動
4. `photometry = viewer.aperture(ra=134.52, dec=25.31, radius_arcsec=5.0)` で測光
5. `astropy.wcs.WCS` オブジェクトと互換性のある WCS を取得

### 受け入れ基準

- `numpy.ndarray[float32]` の入出力が astropy と互換
- `pip install fitsview` で PyPI からインストールできる
- GIL を回避したデータ読み込み（Rust 側で処理）

### 対応機能

PyO3バインディング / maturin パッケージング

---

## UC-24 ブラウザ版での共有・教育利用

**アクター**：P3（教員・学生）
**トリガー**：インストール不要でブラウザから FITS を開いて学生に見せたい
**ゴール**：単一 HTML + WASM でインストールなしに FITS を表示・操作する

### シナリオ

1. GitHub Pages の URL を開く
2. ファイルをドラッグ&ドロップで読み込む
3. ZScale 表示・パン・ズーム・ヘッダ確認が動作する
4. GPU（WebGPU）または CPU フォールバックで描画

### 受け入れ基準

- Chrome 113+ の WebGPU で 60fps
- WebGL フォールバックでも基本操作が動作する
- ローカルファイルのみ扱う（サーバー送信なし）

### 対応機能

Wasm対応 / WebGPU/WebGLレンダラー

---

## ユースケースと実装フェーズの対応表

| UC | タイトル | フェーズ | 優先度 |
|---|---|---|---|
| UC-01 | クイックルック | フェーズ1（完了） | 完了 |
| UC-02 | 画像品質評価 | フェーズ2-3 | 高 |
| UC-03 | 複数ファイル比較 | フェーズ2 | 高 |
| UC-04 | カラーマップ・スケール調整 | フェーズ2 | 高 |
| UC-05 | WCS座標・グリッド | フェーズ2 | 高 |
| UC-06 | アパーチャー測光 | フェーズ3 | 高 |
| UC-07 | RGBコンポジット | フェーズ3 | 中 |
| UC-08 | コントゥアーオーバーレイ | フェーズ3 | 中 |
| UC-09 | カタログオーバーレイ | フェーズ3 | 中 |
| UC-10 | 1Dプロファイル抽出 | フェーズ3 | 中 |
| UC-11 | データキューブ閲覧 | フェーズ2 | 高 |
| UC-12 | X線イベントリスト | フェーズ1（完了） | 完了 |
| UC-13 | DS9リージョンファイル | フェーズ1（完了） | 完了 |
| UC-14 | 画像演算 | フェーズ3 | 中 |
| UC-15 | バッドピクセルマスク | フェーズ3 | 中 |
| UC-16 | セッション管理 | フェーズ2 | 高 |
| UC-17 | アノテーション | フェーズ2 | 中 |
| UC-18 | 論文用図版書き出し | フェーズ4 | 中 |
| UC-19 | パイプラインCI/CD | フェーズ1（完了）+ 4 | 完了/中 |
| UC-20 | 大容量FITS閲覧 | フェーズ1（完了） | 完了 |
| UC-21 | SAMP連携 | フェーズ4 | 低 |
| UC-22 | スクリプトコンソール | フェーズ4 | 低 |
| UC-23 | Pythonライブラリ | フェーズ5 | 低 |
| UC-24 | ブラウザ版Wasm | フェーズ5 | 低 |

---

## 実装されていない主要ユースケース（ギャップ分析）

フェーズ1完了時点で対応できていない重要な業務を優先度順に示す。

### 優先度：高（フェーズ2で対応すべき）

1. **ヒストグラムパネル**（UC-02, UC-04）：vmin/vmax の対話的調整は毎日使われる
2. **連動パン・ズーム**（UC-03）：比較ワークフローを完成させる
3. **WCSグリッドオーバーレイ**（UC-05）：座標確認の基本機能
4. **データキューブ**（UC-11）：IFU・分光データが一切扱えない
5. **セッション管理**（UC-16）：IDEを名乗るなら必須

### 優先度：中（フェーズ3で対応すべき）

6. **アパーチャー測光**（UC-06）：観測天文学者の最重要業務の一つ
7. **1Dプロファイル抽出**（UC-10）：PSF確認・輝度プロファイルに必須
8. **カタログオーバーレイ**（UC-09）：天体同定はVO時代の基本作業
9. **RGBコンポジット**（UC-07）：多バンドの基本可視化
10. **コントゥアーオーバーレイ**（UC-08）：電波/X線天文学者に必須

---

*最終更新：2026-05-31 | 作成者：fits-view プロジェクト*
