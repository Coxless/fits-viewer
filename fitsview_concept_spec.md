# fits-view — コンセプト仕様書

> **"科学データを、科学者のスピードで。"**
> 超軽量・超高速・次世代FITSビューア（Rust製）

> **注記：** プロダクト名 `fits-view` は仮称。正式名称は後日決定予定。

---

## 1. プロジェクト概要

### 背景と課題

天文学における標準データ形式FITSは、1981年の策定以来40年以上にわたり使われ続けている。しかし、既存のビューアツールは以下の問題を抱えている。

| 問題 | 代表的な既存ツール |
|---|---|
| UIが1990年代のまま（Tcl/Tk） | DS9, SAOImage |
| RAM依存で大型ファイルが扱えない | DS9, AstroImageJ |
| GUIが孤立しCLI/スクリプトと連携不可 | ほぼ全ツール |
| 起動が遅い（JVM依存など） | APT, AstroImageJ |
| Wasmに非対応（インストール必須） | 全ツール |
| ディレクトリ単位での作業が困難 | 全ツール |
| データキューブ（3D+）の操作が貧弱 | DS9（限定的） |
| VO/SAMP連携が既存ツール前提 | Aladin, TOPCAT |

SKA（Square Kilometre Array）、Rubin Observatory（LSST）、Subaru HSCなど次世代観測装置は、1ファイルで数十GB〜数TBのFITSデータを生成する。既存ツールはこのスケールに根本的に対応できない。

### ビジョン

**fits-viewは、巨大データを軽快に扱い、CLIやスクリプトと自然に統合できる、天文学者のためのモダンな作業ツールである。**

VSCodeのような直感的なUIでディレクトリを丸ごと開き、複数のFITSファイルをタブで管理しながら、パイプラインの一員として動作できる。さらに、DS9が30年かけて積み上げてきた科学機能（WCS・カタログ・測光・キューブ）をモダンなUXで提供し、真の「FITS IDE」を目指す。

---

## 2. ターゲットユーザー

### プライマリ

- **観測天文学者**：望遠鏡データのクイックルック、品質確認、測光
- **データパイプラインエンジニア**：CI/CDでの自動サムネイル生成、検査
- **大学院生・研究者**：日常的なデータ閲覧・解析プレビュー

### セカンダリ

- 天文ソフトウェア開発者（Pythonライブラリとの統合）
- プラネタリウム・教育機関（Wasm版）

---

## 3. 差別化戦略

### コアバリュー（4本柱）

```
┌─────────────────────────────────────────────────────┐
│  1. 超高速・超軽量     GPU駆動（CPU自動フォールバック）│
│  2. 巨大データ対応     TB級FITSをゼロコピーで         │
│  3. CLI/スクリプト統合  パイプラインの一員として       │
│  4. FITS IDE           DS9の科学機能 + VSCode UX      │
└─────────────────────────────────────────────────────┘
```

### 競合比較

| 機能 | fits-view | DS9 | AstroImageJ | FITS Liberator |
|---|:---:|:---:|:---:|:---:|
| 起動時間 < 1秒 | ✅ | ❌ | ❌ | ❌ |
| TB級ファイル対応 | ✅ | ❌ | ❌ | △ |
| GPU描画（CPU自動FB） | ✅ | ❌ | ❌ | ❌ |
| CLIヘッドレス | ✅ | ❌ | ❌ | ❌ |
| Wasm対応 | ✅（予定）| ❌ | ❌ | ❌ |
| Python連携 | ✅（予定）| △ | ✅ | ❌ |
| セッション保存 | ✅（予定）| △ | ❌ | ❌ |
| ディレクトリ単位で開く | ✅ | ❌ | ❌ | ❌ |
| 複数ファイルのタブ管理 | ✅ | △ | ❌ | ❌ |
| データキューブ操作 | 🔲 | ✅ | △ | ❌ |
| ヒストグラムパネル | 🔲 | ✅ | ✅ | ✅ |
| RGBコンポジット | 🔲 | ✅ | △ | ✅ |
| カタログオーバーレイ | 🔲 | ✅ | △ | ❌ |
| コントゥアーオーバーレイ | 🔲 | ✅ | ❌ | ❌ |
| アパーチャーフォトメトリー | 🔲 | △ | ✅ | ❌ |
| WCSグリッドオーバーレイ | 🔲 | ✅ | △ | ❌ |
| SAMP/VO連携 | 🔲 | ✅ | ✅ | ❌ |
| スクリプトコンソール | 🔲 | △ | △ | ❌ |
| イベントリスト表示 | ✅ | ✅ | ❌ | ❌ |

> ✅ 実装済み　🔲 未実装（本仕様書で計画）　△ 限定的　❌ 未対応

---

## 4. 機能仕様

### 4.1 VSCodeライクUI（実装済み）

fits-viewのGUIはVS Codeの操作感をベースに設計する。

#### 実装済み機能

- ファイルエクスプローラー（左サイドバー、ディレクトリウォッチ付き）
- マルチタブ管理（`Ctrl+Tab` / `Ctrl+W`）
- 分割表示（Single / SideBySide / Grid2x2）
- コマンドパレット（`Ctrl+Shift+P`）
- ヘッダパネル（右パネル、キーワード検索付き）
- 統計パネル（`Ctrl+I`）
- ステータスバー（ファイル名・HDU・ピクセル値・WCS座標・ズーム倍率）
- `fits-view .` でカレントディレクトリを開く

#### レイアウト構成

```
┌──────────────────────────────────────────────────────────┐
│  タブバー（開いているファイル一覧）                         │
├────────────┬────────────────────────────┬────────────────┤
│            │                            │                │
│  ファイル  │       ビューポート          │  ヘッダ        │
│  エクスプ  │      （FITSイメージ）       │  パネル        │
│  ローラー  │                            │                │
│            │                            │  統計パネル    │
│            │                            │                │
├────────────┴────────────────────────────┴────────────────┤
│  ステータスバー（ファイル名 · HDU · (x,y)=val · RA/Dec）  │
└──────────────────────────────────────────────────────────┘
```

#### キーボードショートカット一覧

| 操作 | ショートカット |
|---|---|
| ファイルを開く | コマンドパレット → Open File... |
| ディレクトリを開く | コマンドパレット → Open Directory... |
| タブを閉じる | `Ctrl+W` |
| タブ切り替え | `Ctrl+Tab` / `Ctrl+Shift+Tab` |
| 分割表示（縦） | `Ctrl+\` |
| 分割表示（横） | `Ctrl+K Ctrl+\` |
| サイドバー表示切替 | `Ctrl+B` |
| ヘッダパネル表示切替 | `Ctrl+H` |
| 統計パネル表示切替 | `Ctrl+I` |
| ズームイン/アウト | `Ctrl++` / `Ctrl+-` |
| フィットトゥウィンドウ | `Ctrl+0` |
| HDU切替（前/次） | `[` / `]` |
| コマンドパレット | `Ctrl+Shift+P` |
| クロスヘア切替 | `Ctrl+X` |
| ブリンク切替 | `Ctrl+L` |
| リージョンファイルを開く | `Ctrl+R` |

#### 未実装 UI 機能

- タブのドラッグ&ドロップによる並び替え
- 分割ビューの連動パン・ズーム（リンクモード）
- ファイルエクスプローラーの右クリックコンテキストメニュー
- `--layout side-by-side` 起動オプション

---

### 4.2 巨大データ対応（実装済み）

#### 実装済み機能

- `memmap2` によるOSレベルのメモリマッピング（ファイル全体をRAMに載せない）
- タイルサイズ：512×512px（デフォルト）
- LRUキャッシュによるタイル管理（メモリ上限設定可能）
- LOD（Level of Detail）：ズームレベルに応じた解像度自動選択
- 非同期タイルロード（tokio + rayon）
- 512MB超ファイルの自動検出・切り替え
- 圧縮FITS対応（compressed_fits.rs）

#### 対応ファイル規模の目標

| ファイルサイズ | 動作目標 |
|---|---|
| < 1 GB | 即時表示（< 500ms） |
| 1 GB – 10 GB | 初回タイル表示 < 2秒 |
| 10 GB – 1 TB | パン・ズームはスムーズ、初期読み込みは進捗表示 |
| > 1 TB | タイルストリーミングモード（要設定） |

---

### 4.3 描画・表示（実装済み）

#### スケーリングアルゴリズム

| 名前 | 説明 |
|---|---|
| ZScale | DS9互換。天文画像のデフォルト |
| Linear | 線形スケール |
| Log | 対数スケール |
| Sqrt | 平方根スケール |
| ASinh | 逆双曲線正弦。低輝度の細部保存 |
| MinMax | 全範囲マッピング |
| HistEq | ヒストグラム均一化 |

#### カラーマップ

Gray / Viridis / Plasma / Inferno / Hot / Rainbow

#### その他の表示機能

- コントラスト・バイアス調整（右ドラッグ + スライダー）
- クロスヘアオーバーレイ
- ブリンクモード（複数タブを交互に切り替え）
- DS9リージョンファイル（`.reg`）のオーバーレイ表示
- WCS座標のリアルタイム表示（ステータスバー）
- BINTABLEイベントリスト → カウントマップ変換

---

### 4.4 CLIモード（実装済み）

GUIとCLIが同一バイナリ。サブコマンドの有無でモードを自動判定する。

```bash
fits-view image.fits           # GUI起動
fits-view render <input> ...   # ヘッドレスレンダリング
fits-view info <input> ...     # ヘッダ情報表示
fits-view stats <input> ...    # 統計情報表示
fits-view check <input> ...    # FITS検証（CI/CD用）
```

#### 実装済みオプション

**render:**
- `--output` / `--size WxH` / `--scale` / `--colormap` / `--hdu`

**info:**
- `--hdu` / `--format` (table/json/csv)

**stats:**
- `--hdu` / `--format` (table/json/csv)

**check:**
- `--min-width` / `--min-height` / `--has-wcs` / `--has-keyword`

#### 未実装 CLI 機能

| 機能 | コマンド例 |
|---|---|
| renderのvmin/vmax手動指定 | `--vmin 100 --vmax 4000` |
| renderのバッチ出力 | `--output-dir ./thumbs/ --jobs 8` |
| statsのリージョン指定 | `--region 100:200,100:200` |
| checkの式アサーション | `--assert "NAXIS == 2"` |
| renderのPDF/SVG出力 | `--format pdf` / `--format svg` |

---

### 4.5 ヒストグラムパネル ★新規

天文画像の表示品質を決定づける最重要 UI 要素。ピクセル値の分布を見ながら vmin / vmax を直感的に調整できる。

#### UI 設計

```
┌─────────────────────────────────────────────┐
│  Histogram                              [×]  │
│                                             │
│  ▁▂▄█▇▅▃▂▁▁▁▁▁  ← ピクセル値ヒストグラム    │
│  ↑vmin      vmax↑                           │
│  [──────────────────────────]               │
│  vmin: [  120.0  ]  vmax: [ 4200.0  ]       │
│  [Auto ZScale] [MinMax] [99%] [99.9%]       │
└─────────────────────────────────────────────┘
```

#### 機能仕様

- ピクセル値分布を対数スケールのバーチャートで表示
- vmin / vmax のドラッグ可能なカーソルマーカー
- クリックした領域にvmin/vmaxをスナップする
- 自動計算ボタン：ZScale / MinMax / 99%カット / 99.9%カット
- パーセンタイル表示（1%, 50%, 99%の位置マーカー）
- ヒストグラムのビン数設定（デフォルト 256）
- `Ctrl+H`（ヒストグラム専用）または コマンドパレット経由で表示切替
- タイル表示中はビューポート内の可視タイルのみからサンプリング

---

### 4.6 データキューブ（3D/4D FITS）★新規

分光観測・時系列観測データは `NAXIS >= 3` の多次元FITS配列として格納される（例：`NAXIS1=2048, NAXIS2=2048, NAXIS3=1024` の IFUデータ）。

#### 対象データ型

| データ型 | 典型例 |
|---|---|
| スペクトルキューブ | VLT/MUSE, Subaru/FOCAS, JWST/NIRSpec |
| 時系列イメージ | 測光モニタリング、X線タイムシリーズ |
| 偏光マップ | `NAXIS3=4`（I, Q, U, V） |
| マルチバンドモザイク | HST/WFC3 多フィルター |

#### UI 設計

```
┌────────────────────────────────────────────────────┐
│  キューブパネル                                     │
│  軸: z [波長 ▼]   3: 512  4: 1                     │
│  z = 256 / 1024  λ = 656.3 nm                      │
│  [◀]  ────────●────────────  [▶]  [▶▶ Play]        │
│  Step: [1]  FPS: [10]  Range: [0──1024]             │
└────────────────────────────────────────────────────┘
```

#### 機能仕様

- NAXIS ≥ 3 のFITSファイルを自動検出してキューブモードで開く
- スライダーで第3軸（波長・時間・ストークスパラメータ等）を操作
- `WAV` / `FREQ` / `TIME` 等のWCS軸キーワードを読んで軸ラベルを表示
- 任意の2軸を選択して表示できる（例：`(x,y)`, `(x,z)`, `(y,z)`）
- アニメーション再生（FPS設定可能、ループ/バウンスモード）
- 任意位置のスペクトル抽出（マウスクリックで (x,y) 座標を固定してスペクトルをプロット）
- ファイルをRAMに載せずにスライスをオンデマンドで読む（タイルローダーと統合）
- フレーム平均・合計モード（指定範囲を畳み込んで表示）

---

### 4.7 WCSグリッドオーバーレイ ★新規（計画済み）

RA/Dec の格子線を画像上にオーバーレイ表示する。天文学者が座標感覚を掴むための基本機能。

#### 機能仕様

- 赤経（RA）・赤緯（Dec）の格子線をズームレベルに応じて自動密度調整
- 格子線の交点に RA/Dec ラベルを表示
- 対応投影法：TAN（Gnomonic）, SIN, ZEA, AIT, CAR, MER
  - 現状の実装は TAN のみ。他投影法を追加
- グリッドの色・透明度・線幅の設定
- コマンドパレット → "Toggle WCS Grid" で切替
- グリッドが表示できないファイル（WCSなし）では自動的に無効化

---

### 4.8 RGBコンポジットモード ★新規

3つのFITSファイルをR・G・Bチャンネルに割り当てて疑似カラー合成画像を生成する。多バンド観測の基本的な可視化ワークフロー。

#### UI 設計

```
┌─────────────────────────────┐
│  RGB Composite              │
│  R: [image_r.fits ▼]  [×]  │
│  G: [image_g.fits ▼]  [×]  │
│  B: [image_b.fits ▼]  [×]  │
│  R scale: [ZScale ▼]        │
│  G scale: [ZScale ▼]        │
│  B scale: [ZScale ▼]        │
│  [Apply]  [Reset]           │
└─────────────────────────────┘
```

#### 機能仕様

- 現在開いているタブから R/G/B を選択（ドロップダウン）
- チャンネルごとに独立したスケール・vmin・vmax を設定
- 結果を新しいタブとして表示
- `fits-view render a.fits b.fits c.fits --rgb --output composite.png` のCLI対応
- WCS整合が取れない場合は警告を表示（ピクセル座標でそのまま合成）
- 将来：WCSに基づくリサンプリング・リプロジェクション

---

### 4.9 コントゥアーオーバーレイ ★新規

等高線を画像上にオーバーレイ表示する。電波強度を光学画像の上に重ねるなど、マルチバンド比較の標準的な手法。

#### 機能仕様

- オーバーレイのソース：現在の画像、または別のFITSファイル
- コントゥアーレベルの指定：
  - 自動（sigma倍数：3σ, 5σ, 10σ, 30σ）
  - パーセンタイル（10%, 30%, 50%, 70%, 90%）
  - 手動数値入力
- 色・線幅・透明度の設定
- WCS整合がある場合は座標変換して重ねる
- コマンドパレット → "Add Contour Overlay..."

---

### 4.10 カタログオーバーレイ ★新規

オンライン天文カタログからオブジェクトを取得し、画像上にシンボルとラベルで表示する。

#### 対応カタログ

| カタログ | 用途 |
|---|---|
| SIMBAD | 天体名検索・命名 |
| VizieR | 汎用カタログ検索 |
| Gaia DR3 | 高精度星位置・測光 |
| 2MASS | 近赤外線測光 |
| NED | 銀河外天体 |

#### UI 設計

```
コマンドパレット → "Query Catalog..."
→ ダイアログ：
  カタログ: [SIMBAD ▼]
  半径: [10 arcmin]
  最大件数: [500]
  [Query]
→ 画像上に円または十字のシンボル + ラベル
→ シンボルクリックで詳細ポップアップ
```

#### 機能仕様

- 現在の画像FOV（WCS範囲）を自動認識してクエリ実行
- 結果をオーバーレイとしてタブに保存（ファイル単位）
- オフライン動作時はキャッシュ済みデータを使用
- フィルタリング：等級・タイプ・キーワード
- VOTable形式のローカルファイルもロード可能（`Ctrl+R`と統合）
- 将来：カタログとのクロスマッチ、測光比較

---

### 4.11 1Dプロファイル抽出 ★新規

画像上で直線を引いてその方向の輝度プロファイルをプロット表示する。銀河輝度プロファイルや点像分布関数（PSF）確認に使用する。

#### UI 設計

```
[Line Profile]ボタンを押した後、画像上をドラッグ
→ 下部パネルにプロファイルをプロット：

  値
  │    *
  │   * *
  │  *   *   *
  │ *     * *  *
  └──────────────→ ピクセル距離 / WCS角距離
  
  [Export CSV]  [Gaussian Fit]
```

#### 機能仕様

- マウスドラッグで直線を定義（始点・終点）
- ガウシアンフィット（PSF測定用）
- WCSが有効な場合は距離を角秒・arcmin・度で表示
- 複数プロファイルを重ねて比較
- CSVエクスポート

---

### 4.12 アパーチャーフォトメトリー（基本） ★新規

円形アパーチャーを置いてフラックス積分を行う基本的な測光ツール。

#### UI 設計

```
コマンドパレット → "Photometry Mode"
→ 画像上をクリックで円形アパーチャーを配置：
  ・内円（ターゲット）
  ・外円（バックグラウンド環）

ポップアップ結果：
  Center: (1024.3, 512.7) px  = RA 134.52, Dec 25.31
  Aperture radius: 8.0 px = 2.4"
  Sky annulus: 12–20 px
  Net flux: 12340.5 ADU
  SNR: 45.2
```

#### 機能仕様

- 円形アパーチャー（固定半径 / ガウシアンFWHM自動）
- バックグラウンド環（annulus）によるスカイ差し引き
- 複数アパーチャーの一括配置と測定結果テーブル
- CSV/VOTable形式でのエクスポート
- フラックスの等級換算（FITS ヘッダの `MAGZPT` / `PHOTZP` 参照）
- 将来：楕円アパーチャー、PSFフォトメトリー

---

### 4.13 画像演算 ★新規

複数の FITS ファイル間で算術演算を行い、結果を新しいタブとして表示する。パイプライン前後の差分確認・フラット補正・S/N比マップ生成に使用する。

#### UI 設計

```
コマンドパレット → "Image Arithmetic..."

  A: [image_sci.fits / HDU 0 ▼]
  演算: [A - B ▼]   ← A+B, A-B, A*B, A/B, (A-B)/B
  B: [image_ref.fits / HDU 0 ▼]
  
  スケールファクター: [1.0]
  結果を新タブで開く: [✓]
  [実行]
```

#### 機能仕様

- 演算子：`+`, `-`, `*`, `/`, `(A-B)/B`（相対差）, `A/B` (ratio)
- 画像サイズが異なる場合は警告
- WCS整合チェック（不一致時に確認ダイアログ）
- 結果FITSを保存（`fits-view arithmetic a.fits - b.fits --output diff.fits` CLI対応）
- スカラー演算：`A * 2.0`, `A + 100.0`

---

### 4.14 マスクオーバーレイ ★新規

バッドピクセルマスク・検出マスクを半透明で重ねる。パイプライン品質確認に必要。

#### 機能仕様

- マスクFITS（非ゼロ値 = マスク）を別ファイルから読み込み
- カラー・透明度の設定（デフォルト：赤、50%透過）
- マスク値ごとに色分け（複数ビットマスク対応）
- コマンドパレット → "Load Mask..."（`Ctrl+R` と同ダイアログから）

---

### 4.15 セッション管理（`.fvs` 形式） ★計画済み

```json
{
  "version": "1.0",
  "workspace": "/data/obs/2026-05-25/",
  "files": [
    {
      "path": "image_r.fits",
      "hdu": 1,
      "display": {
        "colormap": "viridis",
        "scale": "log",
        "vmin": 100.5,
        "vmax": 4200.0,
        "zoom": 2.5,
        "center_wcs": [134.5, 25.3]
      },
      "annotations": [
        {"type": "circle", "ra": 134.52, "dec": 25.31, "radius_arcsec": 5.0, "label": "Target"}
      ],
      "catalogs": ["gaia_dr3_query_result.vot"],
      "region_files": ["targets.reg"]
    }
  ],
  "layout": "side-by-side"
}
```

- `fits-view --session obs_20260525.fvs` でセッション復元
- コマンドパレット → "Save Session" / "Open Session"
- 終了時に `~/.config/fits-view/last_session.fvs` へ自動保存
- `.fvs` ファイルはJSONで人間が読みやすい形式

---

### 4.16 論文品質の書き出し ★計画済み

- `fits-view render image.fits --format pdf --dpi 300 --colorbar --title "M87 Ha"`
- 余白・軸ラベル・カラーバー・タイトルの設定オプション
- DPI指定（デフォルト 150dpi、論文用 300dpi）
- SVG出力（ベクター形式、論文投稿・プレゼン用）
- WCSグリッドをPDF/SVGに含める

---

### 4.17 SAMP / Virtual Observatory 連携 ★新規

SAMP（Simple Application Messaging Protocol）はVirtual Observatoryツール間の標準メッセージングプロトコル。TOPCAT・Aladin等との連携を可能にする。

#### 機能仕様

- SAMPハブへの自動接続（ローカルまたはリモート）
- 受信対応メッセージ：
  - `image.load.fits`：FITSファイルを新タブで開く
  - `coord.pointAt.sky`：RA/Decにカーソルをジャンプ
  - `table.load.votable`：カタログオーバーレイとして読み込む
- 送信対応メッセージ：
  - `coord.pointAt.sky`：カーソル座標をTOPCAT等に通知
  - 測光結果テーブルをTOPCATに送信
- メニューバー → "SAMP → Connect"

---

### 4.18 スクリプトコンソール ★新規（IDEの核心機能）

GUIを開いたままPythonコードでFITSデータを操作できる。IDEとしての最大の差別化機能。

#### UI 設計

```
┌─────────────────────────────────────────────────────┐
│  Script Console                               [×]   │
│  ┌───────────────────────────────────────────────┐  │
│  │ >>> import numpy as np                        │  │
│  │ >>> data = fv.active_tab().data()  # np.array │  │
│  │ >>> fv.show(np.log10(data + 1))               │  │
│  └───────────────────────────────────────────────┘  │
│  [Run ▶]  [Clear]  [Export Notebook]                │
└─────────────────────────────────────────────────────┘
```

#### 実装方針

- 組み込みPythonインタープリタ（PyO3経由）またはLua（mlua）
- `fv` オブジェクトがGUIの状態に直接アクセス：
  ```python
  fv.active_tab()          # 現在のタブ
  fv.tabs()                # 全タブ
  fv.show(array)           # numpy配列を新タブとして表示
  fv.active_tab().header() # dict
  fv.active_tab().data()   # np.ndarray[float32]
  fv.active_tab().wcs()    # WCS変換オブジェクト
  ```
- コンソール履歴の保存
- Jupyter Notebookへのエクスポート（`.ipynb`）
- `Ctrl+Shift+C` でコンソールを開く

---

### 4.19 WCS（World Coordinate System）（実装済み・拡張予定）

#### 実装済み

- TAN（Gnomonic）投影のピクセル↔RA/Dec変換
- ステータスバーのリアルタイム WCS 座標表示

#### 追加予定

- WCSグリッドオーバーレイ（§4.7）
- 追加投影法：SIN, ZEA, AIT, CAR, MER
- SIP（Spline Imaging Projection）歪み補正
- `wcslib` C ライブラリとのFFI統合（高精度変換が必要なケース）
- TPV歪みモデル対応

---

### 4.20 Wasm対応（ブラウザ版）★計画済み

- `trunk` + `wasm-bindgen` でブラウザ向けビルド
- `eframe` の `web_app` feature を活用
- `memmap2` は Wasm 非対応のため XHR Range リクエストで代替
- GPU：WebGPU（Chrome 113+）または WebGL フォールバック
- 配布：単一 HTML + WASM ファイル

---

### 4.21 Pythonバインディング（PyO3）★計画済み

```python
import fitsview

img = fitsview.open("image.fits")
header = img.header()              # dict
data = img.data(hdu=1)            # np.ndarray[float32]
viewer = fitsview.show(img, colormap="viridis")
coords = viewer.pick()            # インタラクティブに座標選択
photometry = viewer.aperture(ra=134.52, dec=25.31, radius_arcsec=5.0)
```

- `maturin` でビルド、`PyPI` に公開
- `numpy` / `astropy` との相互運用

---

## 5. 技術スタック

### コア

| 役割 | クレート / 技術 |
|---|---|
| FITSパーサ | `fitsrs`（CDS製、純Rust） |
| メモリマップI/O | `memmap2` |
| GPU描画 | `wgpu`（WebGPU準拠） |
| CPUフォールバック描画 | `rayon` + カスタムソフトウェアレンダラー |
| GUI | `egui` + `eframe` |
| 並列処理 | `rayon` |
| 非同期 | `tokio` |
| CLIパーサ | `clap` v4 |
| シリアライズ | `serde` + `serde_json` |
| ディレクトリウォッチ | `notify` |
| 画像出力 | `image` |

### 追加予定（フェーズ2以降）

| 役割 | 候補 |
|---|---|
| 高精度WCS変換 | `wcslib`（C FFI）または純Rust実装 |
| HTTP/カタログクエリ | `reqwest` |
| SAMPプロトコル | 独自実装（XML-RPC） |
| Pythonインタープリタ | `PyO3` |
| Luaインタープリタ | `mlua` |
| プロットエンジン | `egui_plot` |
| Wasm対応 | `trunk` + `wasm-bindgen` |
| Pythonバインディング配布 | `maturin` |

---

## 6. 実装優先順位

仕様書更新時点（2026-05-29）の実装状況と優先度。

### フェーズ1完了済み（MVP + VSCodeライクUI + 大ファイル + CLI）

- [x] FITS読み込み・スケーリング・カラーマップ・レンダリング
- [x] パン・ズーム・コントラスト/バイアス
- [x] ヘッダパネル・統計パネル・ステータスバー
- [x] ファイルエクスプローラー・タブ管理・分割ビュー
- [x] コマンドパレット・ブリンク・クロスヘア
- [x] DS9リージョンファイル表示
- [x] WCS座標表示（ステータスバー）
- [x] BINTABLEイベントリスト変換
- [x] タイルストリーミング（大容量ファイル）
- [x] GPU/CPUレンダラー自動切替
- [x] CLI：render / info / stats / check

### フェーズ2：科学機能コア（優先度：高）

| 機能 | 理由 |
|---|---|
| ヒストグラムパネル | すべての天文学者が毎日使う基本 UI |
| データキューブ対応 | 分光・IFUデータが扱えない現状は大きな欠落 |
| WCSグリッドオーバーレイ | 座標感覚の把握に必須、実装コスト低め |
| 分割ビューの連動パン・ズーム | 比較機能の完成に必要 |
| セッション管理 | IDEとして不可欠。「再起動したら設定が消える」は致命的 |

### フェーズ3：比較・解析機能（優先度：中）

| 機能 | 理由 |
|---|---|
| RGBコンポジット | 多バンドデータの基本可視化 |
| コントゥアーオーバーレイ | DS9のキラー機能。電波/X線比較に必須 |
| カタログオーバーレイ | VO連携の入口、研究者の毎日のワークフロー |
| 1Dプロファイル抽出 | PSF確認・銀河輝度プロファイルに必要 |
| アパーチャーフォトメトリー | 観測天文学者の基本ツール |

### フェーズ4：IDE・連携機能（優先度：中〜低）

| 機能 | 理由 |
|---|---|
| 画像演算 | 差分確認・フラット補正のクイック実行 |
| マスクオーバーレイ | パイプライン品質検査 |
| CLIギャップの補完 | `--vmin/--vmax`, `--output-dir`, `--assert` など |
| SAMP連携 | 既存VoToolsとの統合 |
| スクリプトコンソール | IDEの核心機能。実装コスト大きいが差別化最大 |
| セッション管理 CLI連携 | `fits-view --session obs.fvs` |
| 論文品質の書き出し（PDF/SVG） | 研究成果への直結 |

### フェーズ5：配布・拡張（優先度：低）

| 機能 | 理由 |
|---|---|
| Wasm対応 | インストール不要のブラウザ版 |
| Pythonバインディング（PyO3） | Astropy/NumPyユーザーとの統合 |
| SIP / TPV WCS歪み補正 | 高精度アストロメトリが必要なユーザー |
| パフォーマンス最適化 | KPI達成 |
| パッケージング・配布 | cargo install / Homebrew / PyPI |

---

## 7. KPI（成功指標）

| 指標 | 目標値 |
|---|---|
| 起動時間（1GB FITSを開くまで） | < 1秒 |
| 10GB FITSの最初のタイル表示 | < 2秒 |
| パン・ズーム操作のフレームレート（GPU） | 60fps |
| パン・ズーム操作のフレームレート（CPU） | 30fps以上 |
| バイナリサイズ（strip後） | < 20MB |
| `fits-view render` スループット | > 50ファイル/分（並列） |
| メモリ使用量（50GB FITSを閲覧中） | < 2GB |
| ヒストグラム描画レイテンシ | < 100ms |
| カタログクエリ（SIMBAD 10arcmin） | < 3秒 |

---

## 8. ライセンス・配布

- **ライセンス**：MIT または Apache-2.0（デュアルライセンス）
- **配布**：
  - バイナリリリース（GitHub Releases）：Linux / macOS / Windows
  - `cargo install fits-view`
  - Homebrew formula（macOS）
  - 将来：`pip install fitsview`（PyO3バインディング同梱）

---

## 9. 参考・関連プロジェクト

| プロジェクト | 関係 |
|---|---|
| [fitsrs](https://github.com/cds-astro/fitsrs) | FITSパーサとして採用 |
| [egui](https://github.com/emilk/egui) | GUIフレームワーク |
| [rerun](https://github.com/rerun-io/rerun) | アーキテクチャ参考（egui + wgpu大規模事例） |
| [VS Code](https://code.visualstudio.com/) | UIデザイン参考 |
| [DS9](https://sites.google.com/cfa.harvard.edu/saoimageds9) | 主要競合・機能参考 |
| [AstroImageJ](https://www.astro.louisville.edu/software/astroimagej/) | 測光機能の参考 |
| [FITS Liberator 5](https://noirlab.edu/public/products/fitsliberator/) | 競合（2025年11月v5.0リリース） |
| [Aladin](https://aladin.cds.unistra.fr/) | VO連携・カタログ参考 |
| [TOPCAT](https://www.starlink.ac.uk/topcat/) | SAMP連携の主要相手 |

---

*最終更新：2026-05-29 | ステータス：フェーズ1完了・フェーズ2計画中*
